# 本地账本 Local Ledger

公开网站、浏览器本地数据模式的多银行记账工具。

- 支持 PDF、CSV、XLSX、XLSM
- 支持 ING、Revolut 专用 PDF 规则
- 支持 ABN AMRO、Rabobank 及其他银行通用表格列识别
- 数据保存在 IndexedDB，不写入服务器
- 支持完整 JSON 备份与恢复

## 隐私说明

网页代码公开托管，但用户导入的银行流水只在浏览器内解析并存储。不同设备、不同浏览器之间不会自动同步。
